# Exact pins, not ranges. Version drift is the usual cause of death for a tutorial nobody
# can reproduce six months after publication.
#
# pyspark and delta-spark MUST be a matched pair — each Delta release targets one Spark minor.
# A mismatch fails at runtime with an unhelpful error, not at install time.
#
# These are installed inside the jobs container (see Dockerfile), which also bakes in the
# Delta and Postgres JDBC jars so the first spark-submit needs no network.
#
# Verified working together on Java 21 / Debian trixie / Python 3.11.

pyspark==4.0.1
delta-spark==4.0.0          # targets Spark 4.0.x — do not bump one without the other
psycopg2-binary==2.9.9
pytest==8.3.4

# Added at build step 9.
# dbt-core==
# dbt-spark[session]==
