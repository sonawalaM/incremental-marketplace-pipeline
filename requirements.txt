# Exact pins, not ranges. Version drift is the usual cause of death for a tutorial
# nobody can reproduce.
#
# ⚠ VERIFY BEFORE PINNING — these move, and I can't confirm the current matrix from here:
#   * pyspark and delta-spark must be a MATCHED PAIR. Delta releases target one Spark minor.
#     Check the Delta release notes for the Spark version each release supports.
#   * Java must be a version your chosen Spark release supports. Confirm with `java -version`.
#   * dbt-spark[session] needs a pyspark matching the one above.
# Once you've confirmed a working combination, replace the ranges below with == pins and
# record the exact versions in the README's "tested with" block.

pyspark==<PIN_ME>
delta-spark==<PIN_ME>          # must match the pyspark minor above
dbt-core==<PIN_ME>
dbt-spark[session]==<PIN_ME>

psycopg2-binary==2.9.9
pytest==8.3.4
